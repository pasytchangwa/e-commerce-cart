import React from 'react'

const reducer = (state, action) => {
  switch(action.type) {
    case 'INCREASE'
    case 'DECREASE'
    case 'REMOVE'
    case 'REMOVE_ALL'
    default
  }
  return (
    <div>reducer</div>
  )
}

export default reducer